/** Internal events that occur inside listr task. */
export enum ListrTaskEventType {
  TITLE = 'TITLE',
  STATE = 'STATE',
  ENABLED = 'ENABLED',
  SUBTASK = 'SUBTASK',
  DATA = 'DATA',
  MESSAGE = 'MESSAGE'
}
