/** Default loglevels for the logger */
export enum LogLevels {
  silent = 'silent',
  fail = 'fail',
  skip = 'skip',
  success = 'success',
  data = 'data',
  start = 'start',
  title = 'title',
  retry = 'retry',
  rollback = 'rollback'
}
