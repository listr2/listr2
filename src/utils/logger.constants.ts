// eslint-disable-next-line @typescript-eslint/naming-convention
export enum logLevels {
  silent = 'silent',
  fail = 'fail',
  skip = 'skip',
  success = 'success',
  data = 'data',
  start = 'start',
  title = 'title'
}
